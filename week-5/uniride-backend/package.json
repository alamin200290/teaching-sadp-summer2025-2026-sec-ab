{
  "name": "uniride-backend",
  "version": "0.5.0-week5",
  "description": "UniRide backend. Week 5: behavioural design patterns (Strategy, Observer, Command, Template Method, State) over the Week 1-4 core.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/src/main.js",
    "demo": "npm run build && node dist/src/demo.js",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "devDependencies": { "@types/express": "^4.17.21", "@types/node": "^22.0.0", "typescript": "^5.6.0" },
  "dependencies": { "express": "^4.21.2" }
}
