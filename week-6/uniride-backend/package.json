{
  "name": "uniride-backend",
  "version": "0.6.0-week6",
  "description": "UniRide backend. Week 6: distributed & cloud-native patterns (Event Broker, Circuit Breaker, Retry, Saga) over the Week 1-5 core.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/src/main.js",
    "demo": "npm run build && node dist/src/demo.js",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "devDependencies": { "@types/express": "^4.17.21", "@types/node": "^22.0.0", "typescript": "^5.6.0" },
  "dependencies": { "express": "^4.21.2" }
}
