{
  "name": "uniride-backend",
  "version": "0.3.0-week3",
  "description": "UniRide backend. Week 3: creational design patterns (Factory Method, Abstract Factory, Builder) over the Week 1-2 layered core.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/src/main.js",
    "demo": "npm run build && node dist/src/demo.js",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "devDependencies": { "@types/express": "^4.17.21", "@types/node": "^22.0.0", "typescript": "^5.6.0" },
  "dependencies": { "express": "^4.21.2" }
}
